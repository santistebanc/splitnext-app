# Decisions

| Id | Decision | Made in | Why |
| --- | --- | --- | --- |
| D-001 | Use remote Supabase project `splitnext-v3` (`ycpkguwfxlhpovnsuujr`, eu-central-1); do not reuse legacy `splitnext` | bootstrap | Fresh empty project matching locked architecture; old project has incompatible schema/functions |
| D-002 | Store access tokens (and device_user_id) in `expo-secure-store` | bootstrap | Long-lived capability secrets; OS keychain/keystore over plain KV |
| D-003 | Dev/demo on physical phone via Expo Go | bootstrap | Real mobile path from WSL; web SQLite is alpha; Expo Go cannot do HTTPS app links (invites later need a dev build) |
| D-004 | Separate `create-group` Edge Function mints first access token; `merge`/`fetch`/`rt-jwt` require it | slice 0001 | Create cannot call merge before a token exists |
| D-005 | Expo app at repo root; `supabase/` alongside | slice 0001 | No monorepo until a second package appears |
| D-006 | `rt-jwt` falls back to `auth_mode: anon_channel` when no mintable JWT secret is configured | slice 0001 | New Supabase projects use ES256 signing keys; legacy JWT secret is not injected into Edge Functions. Capability check still gates subscription; private minted JWT when `JWT_SECRET`/`SUPABASE_JWT_SECRET` is set |
| D-007 | Fetch Edge Function slug is `fetch-entity` (not `fetch`) | slice 0001 | Slug `fetch` produced intermittent HTML/502 responses that broke client JSON parsing |
| D-008 | Create path inserts group via `create-group`; merge→wake→fetch proven via hub bump | slice 0001 | Post-create merge at v1 would be rejected (`version_not_greater`); bump exercises the sync loop |
| D-009 | On group open and app foreground: flush queue + one group fetch (thin missed-wake catch-up) | slice 0002 | Offline bumps must upload without another tap; full missed-wake protocol stays parked |
| D-010 | Foreground sync covers all lobby group ids, not only the open group | slice 0002 | Offline edits on any known group should upload without opening that hub |
| D-011 | Create group stays empty; creator binds via Add member → This is me | slice 0003 | Matches empty-create direction; no auto-member |
| D-012 | On open/foreground, list members + binds for the group (apply by version) | slice 0003 | Thin roster catch-up without full missed-wake protocol |
| D-013 | Slice 0003 hub UI is list + add + This is me + You (Name); rename/leave/rebind parked | slice 0003 | Prove membership spine before chrome |
| D-014 | At most one active bind per device_user_id per group; same member on multiple devices allowed | slice 0003 | Assumed member is per-install; claimed-slot policy stays with invites |
| D-015 | Bind.member_id must reference a member in the same group (composite FK + merge check) | slice 0003 | Prevent cross-group bind via crafted merge |
| D-016 | Outbound queue identity is `entity_type + id + version`; `merge` returns all three per result | slice 0004 | Dropping by bare `id` discarded a pending newer version of the same entity |
| D-017 | `lastError` is a typed `SyncError` `{ code, message, at }`, cleared on every success; old string values are coerced on read | slice 0004 | String prefixes could not be matched on, and stuck red after a good sync |
| D-018 | Flow tests run the real client against a fake `src/api/edge.ts`, importing the server's own `shouldAccept` | slice 0004 | One wire boundary makes flows testable in process; sharing the rule stops the fake from disagreeing with the server |
| D-019 | Flow tests gate the demo gate and slice close, not every commit | slice 0004 | Commits inside a slice are work-in-progress; a per-commit gate gets bypassed rather than obeyed |
| D-020 | Binding stays open until the group's first live expense, then closes for good | slice 0005 | Before there is money in the group a wrong tap costs nothing, so every member stays offerable and the claim can be moved; afterwards expenses are attributed to the payer and moving a bind would silently re-attribute them. |
| D-021 | One bind per device per group: re-binding re-points the existing bind, never adds a second | slice 0005 | Two live binds would make "which member am I?" depend on iteration order. |
| D-022 | Web uses `localStorage` for both secrets and durability, not the wasm sqlite path | slice 0006 | expo-sqlite on web needs `createSyncAccessHandle`, which headless Chromium does not implement — and headless is the environment the web target exists to serve. |
| D-023 | Platform splits use `.web.ts` file resolution, never a `Platform.OS` branch in a caller | slice 0006 | Metro picks the file, so the native module never enters the web bundle and callers stay ignorant of platform. |
| D-024 | Allocations ride inside the expense entity as jsonb, not as child rows | slice 0007 | One version number covers the whole split, so merge cannot accept a new amount while rejecting one of its shares — the entity-level protocol has no transaction to do it with. |
| D-025 | Expenses split equally across the members live at record time, remainder cents handed out in sorted member-id order, frozen into the expense | slice 0007 | Two devices with the same roster in different insertion orders must produce identical allocations or merge flaps; freezing means a member added later joins the next expense, not this one. |
| D-026 | Balances are derived on read from live members + live expenses, never stored | slice 0007 | Nothing to keep in sync and nothing to migrate; the fold is pure and testable. |
| D-027 | Persisted timestamps are repaired at store open, not by changing the persisted format | slice 0007 | Legend revives ISO strings into `Date` before any reviver hook sees them; a format change would leave every already-persisted store broken. |
| D-028 | A slice is a branch → PR gated on tests + typecheck → squash merge → tag `slice-NNNN` on `main` | slice 0008 | The repo now has a remote and CI, so the gate can be enforced rather than remembered; `main` still reads one line per slice. |
| D-029 | The process is vendored into the repo at `.claude/skills/`, with `AGENTS.md` as the entry point | slice 0008 | A clone should carry the loop; depending on a machine-local skill install makes the repo unusable to any other agent. |
| D-030 | The published board is regenerated by CI from `docs/state/`, never served from the committed `docs/slicer.html` | slice 0008 | A stale commit of a generated file can then never become the published truth. |
| D-031 | The web app deploys to the same GitHub Pages site under `/app`, not to a separate host | slice 0008 | No second account and no deploy token beyond the `EXPO_PUBLIC_` values the client ships anyway; the board's "Open the app" link stays same-origin. The trade is no per-PR preview URL — a Vercel or Cloudflare project can be added if that ever earns its keep. |
| D-032 | Board path chips point at GitHub `blob/main`, and only a board served from localhost opens the editor | slice 0008 | The published board has readers with no clone; the editor route was the only one before, which made every chip dead for them. |
| D-033 | `main` takes only squash merges from a PR, with linear history required and the commit built from the PR title + body | slice 0008 | One slice must stay one commit or `git log main` stops being the list of slices and `git revert slice-NNNN` stops reverting a whole one; taking the body from the PR keeps the build commits out of it. |
| D-034 | The Symbols call graph is derived from source at generation time, never authored in `LOGIC.md` | slice 0009 | A `uses` column goes stale within a slice, and the board's own rule is that weight is measured, not asserted. |
| D-035 | Call resolution follows unmapped local helpers transitively within a file, and stops at any name `LOGIC.md` maps | slice 0009 | The map lists deep modules and omits private helpers, so a mapped symbol's own declaration usually holds none of its real calls — `flushQueue` only delegates to `flushQueueInner`. Traversing *through* a mapped name would instead charge its callees to every ancestor. |
| D-036 | Tree expands each symbol once at its shallowest position and stubs every other occurrence | slice 0009 | The graph is a DAG: unfolding every occurrence is 217 nodes for 49 symbols, and a view of the map whose row count does not match the map is not a view of the map. |
| D-037 | Board tooling is tested by `npm run test:board` (Python `unittest`), gated in CI beside `npm test` | slice 0009 | The generator is Python and the suite is Vitest; a derived graph is the one part of the board that can be silently wrong, and porting one function to TS to reach the runner would split the tool across two languages. |
| D-038 | Tree rests closed at its entry points; opening a symbol reveals its own sentence and its children, each closed in turn | slice 0009 | An always-open tree is a longer flat list with indentation — it shows the structure without letting anyone walk it. The fold carries its child count, as the flow cases do, so a closed row still says how much is behind it. |
| D-039 | A shut tree row carries its name and its area only; the whole row is the toggle, and a chevron on the right marks it — solid when children sit under it, hollow when opening reveals just that row's own detail | slice 0009 | Narrows D-038, which had the fold carry its child count in the manner of rule 11: four things on a shut row read as a list rather than a tree, and the count it carried is on the row anyway as `N callers` once open. Solid-versus-hollow keeps the parent/leaf distinction the count was really providing, in shape rather than text, and the row-sized target means the affordance does not have to be the hit area. |
| D-040 | The board is generated, never committed: `docs/slicer.html` is gitignored, built by `npm run board` locally and by CI for Pages | tooling | A tracked render of `docs/state/` produced nothing but rebase conflicts and boards that were stale the moment a state file moved on. Nothing can now publish a copy nobody regenerated. |
| D-041 | `npm run audit` is a merge gate, with findings fatal and notes informational | tooling | The audit existed for a slice and a half without ever running, so the drift it was written to catch accumulated anyway. Splitting the output keeps it gateable: an archive naming a since-removed symbol is history, a `LOGIC.md` row pointing at a moved file is a lie. |
| D-042 | Flows are evidenced by their `.webm` clip, not by a per-flow test panel | tooling | Coverage was read from tests naming `F-` ids and no test ever named one, so the panel never rendered. The capture run already asserts what a flow test would — console clean, state surviving a reload — and fails loudly when it does not. |
| D-043 | A slice opens exactly one PR; `npm run audit` reports a slice that landed as more than one commit on `main` | tooling | Squash-only enforces one commit per pull request, not per slice — slice 0009 landed as three. Nothing can enforce it at merge time, so the rule is stated as a rule and the violation is at least visible afterwards. |
| D-044 | `/code-review`'s result is written into the archive's `### Review` block, and the audit requires the block from slice 0010 on | tooling | The process required the review and recorded it nowhere, which is the argument the Edge paths block already won: an unwritten review is indistinguishable from a skipped one. |
| D-045 | The board generator is split — `state_files.py` parses, `sourcemap.py` attributes, `callgraph.py` derives, `generate-slicer-board.py` renders | tooling | The parts with a right answer were reachable only through a 2794-line render pass, which is the god-file the quality bar forbids in app code. The render is byte-identical across the split. |
| D-046 | `npm run check` is the single gate list; CI runs exactly it | tooling | The four gates were enumerated in AGENTS.md, SKILL.md and ci.yml, three lists that drift apart the moment one gains a fifth. |
| D-047 | `NEXT.md` is reset to a no-slice-picked stub at close | tooling | It otherwise describes a closed slice until the next pick, and the board's *This slice* page presented finished work as work in flight. |
| D-048 | `/code-review` is this repo's own copy and reviews on three axes: `AGENTS.md` invariants, `NEXT.md` as the spec, the quality bar as the standard | tooling | The vendored generic skill hunted for a GitHub issue or a PRD and found neither, so the Spec axis reported "no spec available" every slice while the spec sat in `NEXT.md`. Invariants get their own axis because a reviewer reading for style reads straight past a float holding money. |
| D-049 | The board's renderer is pinned by a golden-file test over a fixture repo, markup only | tooling | Parsers and attribution were tested; the 2000-line render pass that turns their answers into a page was not. Stripping the inline CSS and script keeps a colour tweak from rewriting the fixture. |
| D-050 | `serve-slicer-board.py` binds to localhost and gates `/open` with a per-run token | tooling | The endpoint runs an editor on any path in the repo, and it was bound to `0.0.0.0` so that a Windows-side browser could reach the WSL port. WSL forwards localhost anyway; `--lan` stays for the cases it does not. |
| D-051 | The slice in flight leaves the board: `npm run delta` reports it in the terminal, and CI posts it on the PR from `merge-base...HEAD`. The board's page 04 becomes *Latest slice* — the last closed one, cut to headline, captures and the two deltas | tooling | The in-flight page rendered from the author's uncommitted working tree, so only the author ever saw it and the published board never could. A PR is per-slice (D-043), is where review happens, needs no clone, and measures what landed rather than what is dirty. The board goes back to describing the system as it stands; the archive keeps the plan, review, edge paths and decisions. Superseded in part by D-055: the PR gets a real slice page, not a markdown comment, and the published board drops the Latest slice tab too. |
| D-052 | Merging to `main` is the only way the server moves: CI pushes migrations, redeploys all five Edge Functions stamped with the merge sha, and fails unless every function reports that sha at `?health=1`. No hand deploy, no `workflow_dispatch` | slice 0010 | Hand deploy left the remote missing `expenses` and serving a stale `fetch-entity` for three slices after the archives said otherwise; a silent no-op deploy looked healthy. |
| D-053 | CI authenticates `db push` via the Supabase access token's short-lived login role; `SUPABASE_DB_PASSWORD` is not a required secret | slice 0010 | The database password is never returned by the API after creation, so requiring it would make the first green deploy depend on a human paste; the CLI already mints a login role from the token. |
| D-054 | On Realtime `SUBSCRIBED` after `CHANNEL_ERROR` / `TIMED_OUT` / `CLOSED`, run the same `syncGroup` as open for that group; no cursor | slice 0011 | A socket that dies while the hub stays open never hits D-009's open/foreground triggers; the full missed-wake protocol (a wake log) stays parked |
| D-055 | The current slice is its own slicer page, generated per PR (`--slice-page`), not a tab on the published board. CI hosts it from the `pr-boards` branch and comments the URL. The published board is Overview / Symbols / Flows / Steering only | tooling | D-051 took the in-flight slice off the board but substituted a markdown comment, so a PR still had no slicer frontend. A still of Latest slice was the same gap in a PNG. The page a reviewer needs is the board chrome with only this slice in it — in flight from `NEXT.md` + the branch diff, or closed from the archive the branch added. |
| D-056 | Invite is a hashed one-use 7-day row that names a member; redeem mints a new access token and inserts the bind on the server. `member_id` is not a redeem argument. D-020 is unchanged | slice 0012 | Access tokens are bound to `device_user_id` at mint; a joiner cannot reuse the creator's. Putting the member on the invite row means the joiner cannot retarget the bind, and **This is me** is not needed for them. |
| D-057 | Join is `/join?token=` plus a lobby paste field, not HTTPS app links | slice 0012 | Expo Go cannot open app links (D-003); capture and the published app are already web. |
| D-058 | `push` to `main` or to a `slice/**` branch deploys to `splitnext-v3`. Last green CI wins. Migrations are additive and are not undone if the slice is abandoned. No hand deploy, no `workflow_dispatch`, no wipe | slice 0013 | D-052 still forbids hand deploy and dispatch; it no longer means “only `main`.” A second project was declined (Free-plan slot, and one shared database is enough to demo). |
| D-059 | Every PR comment includes a Camera-scannable QR of the published web app | slice 0013 | Expo Go cannot scan a GitHub URL; the QR is https `/app`. Per-PR app previews stay parked. |
| D-060 | Host is Cloudflare Worker `splitnext` (`splitnext.santistebanc94.workers.dev`): one SQLite Durable Object per `group_id`, D1 `splitnext-index` for `access_tokens` and `invites` only. `splitnext-v3` is left in place, unused. Supersedes D-001 | slice 0014 | Early enough to move; KV cannot transactionally merge versions or one-use invites; a DO is single-threaded per group |
| D-061 | Wake is a hibernating WebSocket on the group Durable Object. `rt-jwt` and the anon_channel fallback are gone. D-006 is obsolete. D-054 still holds: the client maps socket close/error/open onto `CLOSED` / `CHANNEL_ERROR` / `SUBSCRIBED` | slice 0014 | RN `WebSocket` cannot set headers, so the token rides on the query string; hibernation is what keeps Paid-plan DO cost at the floor |
| D-062 | CI is the only way the server moves: `wrangler d1 migrations apply` then `wrangler deploy` stamped with `DEPLOY_SHA`, then `evaluate` fails unless every route in `FUNCTIONS` reports that sha. No hand deploy, no `workflow_dispatch`, no wipe. Retargets D-052. D-053 (Supabase db-password login role) is obsolete | slice 0014 | Same failure D-052 exists to catch; the writer is wrangler instead of the Supabase CLI |
| D-063 | `push` to `main` or to a `slice/**` branch deploys to Worker `splitnext`. Last green CI wins. D1 migrations are additive and are not undone if the slice is abandoned. Retargets D-058 | slice 0014 | Same one-remote rule as 0013; the remote is now the Worker |
| D-064 | Server code lives under `workers/` (wrangler). `supabase/functions` is gone; `supabase/migrations` stay as history. Narrows D-005 | slice 0014 | No monorepo; the Worker is a folder at the Expo root the same way `supabase/` was |
| D-065 | `supabase/` is gone from the repo. Schema history is `workers/migrations` and the slice archives. Supersedes the keep-migrations clause of D-064 | slice 0015 | A leftover tree is something an agent would still run; current-state docs and the audit forbid naming the old host |
| D-066 | Wake statuses are `OPEN` / `ERROR` / `CLOSED`; the current socket retries with backoff. Retargets D-061's mapped names. D-054 still holds: catch-up is still `syncGroup`, no cursor | slice 0016 | Native WebSocket does not rejoin the way Realtime did; D-061's mapped names were the old Realtime words |
| D-067 | Settle-up is derived from balances, never stored; exact min-transfer-count (zero-sum subgroups, then poorest↔richest); rows do not move money | slice 0017 | Same family as D-026; the list is a suggestion, not a ledger entry |
| D-068 | Equal split is over the members selected at record time (default all live); D-025's remainder, sort, and freeze still apply to that set. Payer need not be in the set. | slice 0018 | The add form can omit the payer from the share, which is the settlement shape; D-025's "everyone live" was the only option before a picker existed |
| D-069 | Tapping a settle-up row opens the new-expense form prefilled with that transfer; the tap does not record or move money. Recording is still `addExpense`. Narrows D-067's "rows do nothing" | slice 0019 | The form is the confirm; D-067's derived-not-stored rule is unchanged |
| D-070 | The HTTP contract is vitest against a local Worker (`createTestHarness`), gated in `npm test`. Narrows D-018 (the fake `edge.ts` server is gone) without reviving it. Does not retarget D-019. Never hits `workers.dev` from vitest | slice 0020 | Capture against the remote cannot fail a PR on a shape drift, and CI must not need a live Cloudflare account |
| D-071 | The local-Worker vitest covers the wake wire: query-string auth on `/wake/:groupId` and `{ event: 'wake', payload }` after an accepted merge. Narrows D-070 (which was the POST routes). `/wake/` stays off `FUNCTIONS`. Does not drive `startWakeSubscription` | slice 0021 | Two phones cannot fail a PR on a tip-shape drift; GET+Upgrade is the work, so wake is not a health route |
| D-072 | `npm run check` remains the four fast gates (test + typecheck + test:board + audit). Capture is a second required CI job against a local Worker. Narrows D-046's "CI runs exactly it". Does not retarget D-070 (vitest still never hits `workers.dev`; this job also does not) | slice 0022 | A broken Create group cannot fail a PR while capture stays a laptop ritual against the deployed Worker, and folding Playwright into `check` would make every laptop wait on Metro |
| D-073 | Hub home is the balance list; a member's outgoing transfers live on that member's screen; expenses live behind All expenses. Settle tap still prefills via `settlementHref` and does not record (D-069). | slice 0023 | Member-first chrome from hub-chrome.html; D-067's derived-not-stored rule is unchanged |
| D-074 | Member buckets are one line per expense; the amount is that member's net on that expense (amount paid minus own share, or minus own share if they did not pay). Lines sum to the member's balance. Paid-for counterpart names are the other live allocated members, sorted by member id; owe-for names the payer. | slice 0024 | The hub-chrome prototype only had two-person expenses (`Dinner · Alex`); one line per other person would explode on a six-way split, and hiding settlements would make the buckets disagree with the net. |
| D-075 | Leave unbinds this device (soft-delete the bind, flushed while the token still works) then revokes its access token. The member slot and expenses remain. D-020 is unchanged. | slice 0025 | Parking and the prototype copy assume you can leave with outstanding balances; reclaiming the slot is a later slice. |
| D-076 | Assumed member is set at create or join and cannot be changed. A second target is refused (`binding_locked`). Leave tombstones the bind so this install may bind again. Reverses D-020 and the re-point half of D-021. D-014 uniqueness and D-075 unbind-then-revoke still hold. | slice 0027 | A wrong tap after money exists used to be the reason to lock; the product is now that you name yourself on the way in and do not retarget. Rename and expense edit are how a typo or a split is fixed. |
| D-077 | Create group is a form (name, your name, currency) that writes the group, the creator member, and this device's bind, then opens the hub. Reverses D-011. | slice 0027 | Empty-create plus This is me was the 0003 path; first-run is now one form that already knows who you are. |
| D-078 | Settings is group name, currency, Done, and Leave group. No roster, no Invite, no add member. Leave's unbind-then-revoke is still D-075; the surface moved off You-detail. | slice 0027 | Members and Invite belong on the hub (names) or on unclaimed member detail; Settings is the group's own fields plus leaving. |
| D-079 | Assumed member is labelled You, without a parenthetical display name. Other members stay their name (or (unnamed)). The member header shows the actual name (with edit); the body does not repeat it. Narrows the You (Name) chrome of D-013. | slice 0028 | The name lives on the member, not in the You marker; rename does not need to rewrite the hub row for this device. |
| D-080 | Invite share URL is `{origin}{/pages-prefix}/j/{token}`. The secret is 8 random bytes as 11-char base64url. `/join?token=` still redeems. Narrows D-057. | slice 0028 | The old UUID-length token and `?token=` query made the copied link too long for the invite row. |
| D-081 | Display money with the currency's symbol; the store keeps the ISO code. Unknown codes stay as typed. | slice 0028 | ISO in the picker and on the wire is the stable id; the person reads a glyph. |
| D-082 | A member's display name is editable at the next member version. Bind, expenses, and the assumed-member mapping stay. Unparks the rename clause of D-013. | slice 0028 | A typo after create or join is a rename, not a rebind (D-076). |
| D-083 | An expense is editable at the next version. Allocations are rebuilt equally among the checked live set (D-068 / D-025). Same id; bind and other expenses stay. | slice 0029 | D-076's other half: a wrong split is an edit, not a second expense. |
| D-084 | Keep this app's surfaces. Take from v1: mixed splits, kick, expense delete, pending badge, per-member invite status/resend, activity feed + toast + undo, push, last-opened group, invite landing, legal. Do not take: create-with-full-roster, group-wide invite, short join code, changing who I am (D-076). Push leaves the non-goals list; a marketing site stays out. | steering after 0029 | User restated the rebuild against v1. |
| D-085 | An allocation stores `share_units` and optional `fixed_cents` as well as frozen `amount_cents`. Cents come from `allocateMixed` (fixed off the total first, remainder by shares, leftover by largest remainder then member id). Equal 1-share is the default editor state. Narrows D-025 / D-068 / D-083: edit rebuilds from that intent, not always equally. Pre-0030 cents-only rows hydrate as 1 share each. | slice 0030 | v1 mixed splits on this form. |
| D-086 | Destructive confirmations (Leave group, Delete expense) use a bottom confirm drawer (`ConfirmDrawer` + `InFrameOverlay`), same chrome as payer and currency pickers — not inline blocks on the screen. | slice 0032 | Consistent drawer pattern; inline confirm duplicated styles on two screens. |
| D-087 | Kick soft-deletes a member at the next version. You and members with a live bind cannot be removed from this device. Expenses and binds are not rewritten; live lists exclude tombstones. | slice 0033 | v1 kick on member detail; claimed slots must leave first (D-075). |
| D-088 | Activity is a merge entity flushed after expenses. This slice records `expense_added` when this device adds a cost and has an assumed member. Hub shows the last three events; **View all events** opens `/group/[id]/activity` (not a drawer). Expense description is styled inline. Other event kinds, toast, push, and undo stay parked. | slice 0034 | Foundation for toast/push/undo; burger drawer from hub-chrome was rejected in favour of recent section + page. |
| D-089 | Activity lines show `updated_at` as relative time (`just now`, `5m ago`, …) via `L-relativeTime` / `L-activityRow`. Hub toasts when sync while mounted brings someone else's `expense_added` (`L-activitiesFromOthers` / `L-activityToast`, top banner, tap → Activity). Wake messages run full `L-syncGroup`, not a single-entity fetch. | slice 0035 | Toast needs expense + activity together; relative labels refresh every minute. |
| D-090 | Activity records edit, delete, kick, and rename on the jobs that already perform those actions (`expense_edited`, `expense_deleted`, `member_kicked`, `member_renamed`). Activities table gains optional `member_id` for member-targeted kinds. Formatters still read tombstoned expenses/members for delete/kick lines. Join/leave/group-rename events stay parked. | slice 0036 | Completes mutating-event kinds on the 0034 spine; toast and feed pick them up automatically. |
| D-091 | Persist `last_opened_group_id` in secure storage when `L-openGroup` runs. The lobby auto-replaces to that hub once per app session when the id is still in `L-lobbyIds`; tapping Home later shows the list. Leaving removes a matching last-opened id. | slice 0037 | v1 convenience without hiding the lobby behind every home tap. |
| D-092 | Expo push tokens register on hub open (native only). Worker stores tokens in D1 and sends Expo Push to non-actor devices after an accepted activity merge. Actor device and devices bound to the actor member are excluded. Web skips registration. `EXPO_ACCESS_TOKEN` on the Worker enables delivery; without it fan-out is a no-op. | slice 0038 | Off-hub awareness complementing wake + toast; per-group mute stays parked. |
| D-093 | GitHub Pages production is the public landing at `/` plus the Expo export at `/app`. The slicer board is a local dev tool (`npm run board:serve`) and is not published. The phone bezel lives on `/try` (iframe of `/app`); the app itself is full-bleed. Narrow `/try` windows redirect to `/app`. Store listings stay parked. Narrows D-031 (same host, landing not board) and D-040 (board still generated, no longer by CI for Pages). Reverses D-084's "marketing site stays out" for this landing only; invite `/j/{token}` and legal stay parked. | slice 0039 | Board is internal; visitors should see the product. |
| D-094 | Manual testing is Expo Go (`npm start`) or the landing phone frame (`npm run landing`). `npm run web` stays the full-bleed capture target; capture and CI never drive `/try`. | slice 0039 | The framed site is what a visitor sees; capture needs a 420 viewport without a bezel. |
| D-095 | Undo of add, delete, and kick lives on the Activity page, actor-only, from `undo_snapshot` on that activity. Restore uses the current entity's version + 1. An add whose expense was edited after refuses. Edit, rename, join/leave, and hub-recent Undo stay parked. Events recorded before this slice have no snapshot and cannot undo. | slice 0040 | One snapshot per undoable event keeps the split (D-024) and version merge (incoming wins when newer) intact. |
| D-096 | Public `/j/{token}` on GitHub Pages is the static invite landing (`landing/404.html`), not Expo. Desktop continues to redeem at `/app/j/{token}`. Copied web share URLs use the site prefix (`/splitnext-app/j/{token}`), not `/app`. A short or missing token, and other unknown paths, are a generic 404. Store listings and legal stay parked. Narrows D-080 (share path) and D-093 (invite landing no longer parked). | slice 0041 | Visitors hitting the copied link should see SplitNext chrome before redeem; Expo `/app/j/…` stays the join screen. |
| D-097 | Public privacy and terms live at `/privacy` and `/terms` on the landing host. The landing, invite 404, and `/try` footers link them. Copy matches real behaviour: no accounts, capability tokens, what is stored. Deletion-on-request is a GitHub issue, not a join-code wipe, and is not a self-serve pipeline. Store listings stay parked. Narrows D-093 and D-096 (legal no longer parked). | slice 0042 | Visitors should read what the app actually stores before a store listing exists. |
| D-098 | Expo Go (`appOwnership === 'expo'`) does not import `expo-notifications`; registration and tap-to-open are no-ops there. Development and store builds still register on hub open. Web still skips via `.web.ts`. Narrows D-092 (native no longer includes Expo Go). Per-group mute stays parked. | slice 0043 | SDK 53 throws on that import in Expo Go Android; D-094 phone testing cannot use a development build every time. |
| D-099 | **Pending** on All expenses means that expense id is still in the outbound queue (any version). Hub rows, member buckets, and non-expense queue items stay unbadged. Narrows D-084 pending badge. | slice 0044 | v1 showed unsynced state; this slice scopes the badge to the expense list only. |
| D-100 | The hub has no stack header; Home and Settings sit on the canvas (Home still opens the lobby). **View all events** and the activity toast expand the full list on the hub; **Close** returns. `/group/[id]/activity` remains a URL. Undo stays on that feed. Narrows D-084 (hub chrome), D-088 (activity is not a pushed page from the hub), D-089 (toast tap expands in place). | slice 0046 | First hub-as-shell slice: headerless hub + activity expand. |
| D-101 | Tapping a balance or roster name on the hub expands `L-memberDetail` on the hub; **Close** returns. Kick calls `onClose` instead of stack replace. `/group/[id]/member/[memberId]` remains a URL wrapper with no stack header. Narrows hub-as-shell member accordion and D-084 (member from the hub is not a pushed page). | slice 0047 | Second hub-as-shell slice: member expand in place, same pattern as D-100 activity. |
| D-102 | Settings gear on the hub expands `L-settingsPanel`; **Close** returns without saving; **Done** saves and closes. Leave success still replaces the lobby. `/group/[id]/settings` remains a URL wrapper with no stack header. Narrows hub-as-shell settings overlay. | slice 0048 | Third hub-as-shell slice: settings expand in place. |
| D-103 | **View all expenses** expands `L-expensesPanel` on the hub; **Close** returns. `/group/[id]/expenses` remains a URL wrapper. | slice 0049 | Fourth hub-as-shell slice. |
| D-104 | FAB, expense rows, and settle prefill expand `L-expenseForm` on the hub; save/delete/**Close** return (settle save returns to `L-memberDetail`). `/expense/new` and `/expense/[id]` remain URL wrappers. | slice 0050 | Fifth hub-as-shell slice. |
| D-105 | Home on the hub expands `L-lobbyPanel` as a group switcher with **Close**; picking a group `replace`s the hub. `/` remains the standalone lobby without Close. Narrows D-091. Completes hub-as-shell. | slice 0051 | Sixth hub-as-shell slice; lobby-as-switcher delivered. |
| D-107 | **Undo** for add/delete/kick also shows on hub **Recent activity** rows (same `L-undoActivity` / `L-planUndo` rules as the expanded feed). Edit/rename undo and toast undo stay parked. Narrows D-095 (hub-recent Undo no longer parked) and D-100 (Undo is not feed-only). | slice 0056 | Actor should reverse a mistake without opening **View all events**. |
