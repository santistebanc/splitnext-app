# No slice picked

The last slice closed and the next one has not been chosen yet.

Run `/slicer` to pick it: Phase 1 scores `PARKING.md` risk-first (foundation-risk > core value > breadth > polish) and proposes the top three with a recommendation. This file is overwritten with that slice once it is agreed — until then the board's *Newest slice* page shows the last closed slice, which is exactly what the app is right now.
